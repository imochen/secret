#! /usr/bin/env node

const boxen = require('boxen');
const secret = require('../index.js');

const result = new Array(6).fill().map(() => secret());

console.log(boxen(result.join('\n'), {
  padding: 1,
  borderColor: 'magenta'
}));